version: "3.7"
services:
  mitra-dev:
    container_name: mitra-frontend-dev
    build:
      context: .
      dockerfile: docker/dev/Dockerfile
    volumes:
      - ".:/app"
      - "/app/node_modules"
    ports:
      - "8080:8080"
